#!/bin/bash

echo "START 01_compile"
set -e
BASE_DIR=$1

cd ${BASE_DIR}/apps

echo "do nothing here, do in checkMBatchUtils instead, since DscCR needs everything else installed"

echo "FINISHED 01_compile"

